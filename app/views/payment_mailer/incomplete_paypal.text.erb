<%=render :partial => "email_layouts/header" %>

Sevgili <%=@tmp_payment.name%>,

<% if @page %>

<%=@page_owner.name%> tarafından "<%=@project.name%>" için oluşturulan "<%=@page.title%>" sayfasına bağış yapmak istediğini ancak bağışını tamamlayamadığını farkettik.

<% elsif @project %>

"<%=@project.name%>" projesine bağış yapmak istediğini ancak bağışını tamamlayamadığını farkettik.

<% else #organization%>

"<%=@organization.name%>" kurumuna bağış yapmak istediğini ancak bağışını tamamlayamadığını farkettik.

<%end%>

Benvarim bağışlar için 100% güvenli ödeme sistemi PayPal'ı kullanmaktadır. PayPal hakkında daha fazla bilgiye bu adresten ulaşabilirsin.
<%=paypal_path(nil, :only_path => false)%>

Aşağıdaki adresi tıklayarak bağışını güvenle tamamlayabilirsin.
<%=payment_retry_path(@tmp_payment, @tmp_payment.retry_key, :only_path => false)%>

Eğer kredi kartınızla ilgili bir problem yaşıyorsan lütfen bizimle iletişime geç. (<%=new_contact_form_path(nil, :only_path => false)%>)


Sevgilerimizle.
Benvarim.com Ekibi

<%=render :partial => "email_layouts/footer"%>